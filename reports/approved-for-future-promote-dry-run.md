# Phase 5C-2C-C5N4 — Approved-for-future-promote Dry-run Report

**Phase:** 5C-2C-C5N4
**Mode:** approved_for_future_promote_dry_run_only
**Generated at:** 2026-06-15T11:40:00+08:00
**Base commit:** 6c09d94 (C5N3)
**Status:** PASS — dry-run-only, NOT enabled

---

## 1. STATUS

PASS — dry-run only. `real_approval_allowed=false`, `real_promote_allowed=false`. 15/15 validations PASS. 29/29 new validator checks PASS. Smoke test PASS. Protected paths md5-verified unchanged.

## 2. WHAT_CHANGED

**Added:**
- `dashboard/daily-digest-approval-dry-run-policy.json` — dry-run policy (allowed_dry_run_transition={from: human_review_pending, to: approved_for_future_promote}; blocked_transitions; confirm_phrase, env_gate, etc.)
- `dashboard/daily-digest-approval-dry-run.json` — generated by planner (would_approve=true, real_approval=false).
- `scripts/daily-digest-approval-dry-run.ts` — dry-run planner (no env reads, no network, no production writes, no approval-state writes; prefers config over status for current_state).
- `scripts/validate-daily-digest-approval-dry-run.ts` — 29-check validator.
- `reports/approved-for-future-promote-dry-run.md` — human-readable plan.

**Modified:**
- `scripts/control-server.ts` — added `GET /api/daily-digest/approval-dry-run` (read-only) and `POST /api/daily-digest/human-approval/approval-dry-run` (token + phrase gated, dry-run only).
- `dashboard/control.html` — added Approval Dry-run panel + JS loader.
- `package.json` — added 2 scripts (`check:`, `validate:`).
- `docs/PRIVATE_CONTROL_SERVER_RUNBOOK.md` — added C5N4 section.
- `README.md` — added C5N4 row.
- `ROADMAP.md` — added C5N4 row.

## 3. CURRENT_STATE

- `current_state` = `human_review_pending` (set by C5N3, read from `daily-digest-human-approval-state.json` config)
- `proposed_next_state` = `approved_for_future_promote` (dry-run only)
- `transition` = `{from: "human_review_pending", to: "approved_for_future_promote"}`
- `would_approve` = `true` (11/12 evidence met; 1 missing = `confirm_phrase_matched` which is false in the default CLI call)

## 4. PROPOSED_TRANSITION

The dry-run proposes the transition `human_review_pending → approved_for_future_promote` but does NOT actually perform it. To perform a real transition, a future phase (C5N-5) would be required with:
- A real human-approver runner (CLI or web UI)
- env gate verification (`CQA_DAILY_DIGEST_CONTINUOUS_PROMOTE=1`)
- Per-transition audit log entry

## 5. WOULD_APPROVE

- `would_approve` = `true`
- 11/12 evidence met (only `confirm_phrase_matched` is false in default CLI call)
- The 11 met evidence items confirm that all the upstream conditions are satisfied (previous state, C5N3 history record, C5N3 human review recorded, all dry-run gates, real_approval_allowed=false, production_write_allowed=false, telegram_send_allowed=false, etc.)

## 6. REAL_APPROVAL_STATUS

- `real_approval` = `false` (always)
- `real_approval_allowed` = `false` (policy)
- No approval was granted. This is a dry-run only.

## 7. REAL_PROMOTE_STATUS

- `real_promote_allowed` = `false` (always; enforced in policy and planner)
- `production_write_allowed` = `false` (always)
- No promote was triggered. No production paths were written.

## 8. BLOCKED_TRANSITIONS

- `approved_for_future_promote → promote` (always blocked)
- Any automatic approval (always blocked)
- Any unattended promote (always blocked)
- Any unattended transition (always blocked)

## 9. API_ENDPOINTS

### `GET /api/daily-digest/approval-dry-run`

Read-only. Returns the dry-run result JSON.
- Tested during smoke test: returned `phase=5C-2C-C5N4`, `mode=approved_for_future_promote_dry_run_only`, `would_approve=True`, `real_approval=False`, `production_write_allowed=False`, `current_state=human_review_pending`, `proposed_next_state=approved_for_future_promote`, `transition={from: human_review_pending, to: approved_for_future_promote}`, evidence met 11/12.

### `POST /api/daily-digest/human-approval/approval-dry-run`

Token-gated + confirm-phrase-gated. **Re-runs the dry-run planner only**. Never approves, never promotes, never writes production, never sends Telegram.
- Token check came first in the smoke test (server has a real token configured in `.control.local`); the placeholder `"***"` was rejected with `Forbidden: Invalid or missing control token` (this is expected behavior, not a defect).
- Planner was successfully invoked via CLI: `npx tsx scripts/daily-digest-approval-dry-run.ts` → `would_approve=true`, `proposed_next_state=approved_for_future_promote`, dry-run result written.
- Audit log: `action_id=daily_digest_approval_dry_run`, `risk_level=low`, `real_execution=false`. No token recorded. ✅

## 10. VALIDATION_RESULTS (15/15 PASS)

| # | Suite | Status |
|---|---|---|
| 1 | `validate:daily-digest-approval-dry-run` | ✅ PASS (29/29) |
| 2 | `validate:daily-digest-human-review-pending` | ✅ PASS (26/26) |
| 3 | `validate:daily-digest-human-approval-transition-dry-run` | ✅ PASS (28/28) |
| 4 | `validate:daily-digest-human-approval-scaffold` | ✅ PASS (26/26) |
| 5 | `validate:daily-digest-continuous-promote-workflow` | ✅ PASS (27/27) |
| 6 | `validate:dashboard-control-safety` | ✅ PASS (12/12) |
| 7 | `dashboard:control:validate` | ✅ PASS (16/16) |
| 8 | `validate:daily-digest-promote-approval-pack` | ✅ PASS |
| 9 | `validate:daily-digest-promote-executor-disabled` | ✅ PASS |
| 10 | `validate:daily-digest-promote-execution-review` | ✅ PASS |
| 11 | `validate:daily-digest-promote-gate` | ✅ PASS |
| 12 | `validate:sanitizer-secret-completeness` | ✅ PASS |
| 13 | `validate:sanitizer-false-positives` | ✅ PASS |
| 14 | `validate:telegram-sanitizer` | ✅ PASS |
| 15 | `validate:project-report-send` | ✅ PASS |

## 11. SMOKE_TEST_RESULT

- Started control server on `127.0.0.1:18795` (in background).
- `GET /health` → `200 OK`, mode `localhost-only-dry-run-safe-readonly-confirmed-low-risk-hardened`.
- `GET /api/daily-digest/approval-dry-run` → `200 OK`, returned `phase=5C-2C-C5N4`, `mode=approved_for_future_promote_dry_run_only`, `would_approve=True`, `real_approval=False`, `production_write_allowed=False`, `current_state=human_review_pending`, `proposed_next_state=approved_for_future_promote`, `transition={from: human_review_pending, to: approved_for_future_promote}`, evidence met 11/12.
- `POST /api/daily-digest/human-approval/approval-dry-run` with placeholder `"***"` token → `result=blocked`, `blocked_reason=invalid_token` (token check came first). Audit: `confirm_ok=false`, `result=blocked`. No token recorded.
- Shutdown server cleanly. Port 18795 free.
- **No production paths written** during smoke test.
- **No Telegram sent** during smoke test.
- **C5N1/C5N3 approval state NOT modified** (still `human_review_pending`).

## 12. PROTECTED_PATH_CHECK (md5-verified unchanged)

| Path | Hash (C5M1A baseline) | Current | Status |
|---|---|---|---|
| `reports/daily-digest.md` | `735002a3969746aefabc57c75b5220e8` | `735002a3969746aefabc57c75b5220e8` | ✅ UNCHANGED |
| `reports/telegram-digest.txt` | `53c2a73d440eb32967d1a9185763a6b3` | `53c2a73d440eb32967d1a9185763a6b3` | ✅ UNCHANGED |
| `dashboard/status.json` | `d98c500cb52c78f57edd941cdedc7b49` | `d98c500cb52c78f57edd941cdedc7b49` | ✅ UNCHANGED |

No production data was modified. C5N1/C5N3 approval state was NOT modified. No rollback triggered (none needed).

## 13. MODEL_CALL_STATUS

NONE — pure JSON + TS source edits. Planner uses `fs` + `path` only.

## 14. GENERATED_MEDIA_STATUS

NONE — no images / videos / music generated.

## 15. LIMITATIONS

1. The planner does **not** read `process.env`, so `env_gate_evaluated` is conservatively reported as `false` in the result JSON. A real orchestrator (future phase) would verify the env var externally.
2. The `confirm_phrase_matched` evidence is `false` in the default CLI call (no phrase provided). To exercise the dry-run with the right phrase, the POST endpoint must be used with the exact phrase "DRY RUN DAILY APPROVAL".
3. The C5N1 status JSON (computed) is stale relative to the C5N3 config (modified). The planner prefers the config over the status for `current_state` to avoid this staleness.
4. Even at `approved_for_future_promote` in a future phase, the actual production write is blocked by `production_write_allowed=false` and would require the C5M1 controlled promote executor to be invoked separately.
5. The 29-check validator uses precise regex patterns to avoid false positives from property names like `no_child_process: true` (a property flag) vs actual `child_process` module usage.
6. The dry-run result file is separate from the C5N1/C5N3 approval state files. The dry-run planner only writes to the dry-run result file; it NEVER writes to the C5N1/C5N3 state file.
7. The smoke test ran on port 18795 (not the default 18791) to avoid conflicts. The server was cleanly shut down.

## 16. NEXT_PHASE_PROPOSAL

- **C5N-5 (proposed only, not implemented)**: Real human-approval transition. Would require:
  - A new CLI: `cqa-approve --run-id=<run> --decision=<approved|rejected> --approver=<name> [--reason=<text>]`
  - The CLI would write an audit log entry with `real_approval=true` and `real_execution=true` ONLY AFTER the human-confirmed the env gate + checked the approval pack + verified the from/to state pair
  - The CLI would flip the approval_state to the new state and write a new transition history record
  - A new endpoint: `POST /api/daily-digest/human-approval/grant` (gated by token + confirm phrase + env gate)
  - All transitions remain manual-initiation gated; no timer / cron / auto-trigger.

## 17. Files Added / Modified

**Added:**
- `dashboard/daily-digest-approval-dry-run-policy.json`
- `dashboard/daily-digest-approval-dry-run.json`
- `scripts/daily-digest-approval-dry-run.ts`
- `scripts/validate-daily-digest-approval-dry-run.ts`
- `reports/approved-for-future-promote-dry-run.md`
- `reports/telegram-phase-5c2c-c5n4-approval-dry-run.txt`

**Modified:**
- `scripts/control-server.ts` (+2 endpoints)
- `dashboard/control.html` (+1 panel + JS loader)
- `package.json` (+2 scripts)
- `docs/PRIVATE_CONTROL_SERVER_RUNBOOK.md` (+1 phase section)
- `README.md` (+1 row)
- `ROADMAP.md` (+1 row)

## 18. Commit

- Branch: `master`
- Base commit: `6c09d94` (C5N3)
- New commit (this phase): `git commit -m "Phase 5C-2C-C5N4: Add approved-for-future-promote dry-run"`
- Push: `origin/master`

---

*辛 🔮 — 实操优先，落地为王。Phase 5C-2C-C5N4 完成。*
