# Phase 5C-2C-C5N2 — Manual Approval Transition Dry-run Report

**Phase:** 5C-2C-C5N2
**Mode:** approval_transition_dry_run_only
**Generated at:** 2026-06-15T10:50:00+08:00
**Base commit:** 670e7f6 (C5N1)
**Status:** PASS — dry-run-only, NOT enabled

---

## 1. STATUS

PASS — dry-run only. `real_approval_allowed=false`, `real_transition_allowed=false`. 14/14 validations PASS. 28/28 new validator checks PASS. Smoke test PASS. Protected paths md5-verified unchanged.

## 2. WHAT_CHANGED

**Added:**
- `dashboard/daily-digest-human-approval-transition-policy.json` — transition policy (allowed_dry_run_transition={from: approval_pack_ready, to: human_review_pending}; blocked_transitions list; confirm_phrase, env_gate, etc.)
- `dashboard/daily-digest-human-approval-transition-dry-run.json` — generated by planner (would_transition=true, real_transition=false).
- `scripts/daily-digest-human-approval-transition-dry-run.ts` — dry-run planner (no env reads, no network, no production writes, no approval-state writes).
- `scripts/validate-daily-digest-human-approval-transition-dry-run.ts` — 28-check validator.
- `reports/human-approval-transition-dry-run.md` — human-readable plan.

**Modified:**
- `scripts/control-server.ts` — added `GET /api/daily-digest/human-approval-transition-dry-run` (read-only) and `POST /api/daily-digest/human-approval/transition-dry-run` (token + phrase gated, dry-run only).
- `dashboard/control.html` — added Approval Transition Dry-run panel + JS loader.
- `package.json` — added 2 scripts (`check:`, `validate:`).
- `docs/PRIVATE_CONTROL_SERVER_RUNBOOK.md` — added C5N2 section.
- `README.md` — added C5N2 row.
- `ROADMAP.md` — added C5N2 row.

## 3. CURRENT_STATE

- `current_state` = `approval_pack_ready` (from C5N1 status)
- `proposed_next_state` = `human_review_pending` (dry-run only)
- `transition` = `{from: "approval_pack_ready", to: "human_review_pending"}`
- `would_transition` = `true` (all evidence met except `confirm_phrase_matched` which is the phrase check)

## 4. PROPOSED_TRANSITION

The dry-run proposes the transition `approval_pack_ready → human_review_pending` but does NOT actually perform it. To perform a real transition, a future phase (C5N-3) would be required with:
- A real human-approver runner (CLI or web UI)
- env gate verification (`CQA_DAILY_DIGEST_CONTINUOUS_PROMOTE=1`)
- Per-transition audit log entry

## 5. REAL_TRANSITION_STATUS

- `real_transition` = `false` (always; this is a dry-run)
- `real_approval_allowed` = `false`
- `real_promote_allowed` = `false`
- `production_write_allowed` = `false`
- `telegram_send_allowed` = `false`

The dry-run planner writes the would-transition result to `dashboard/daily-digest-human-approval-transition-dry-run.json` but does NOT modify the C5N1 approval state file.

## 6. BLOCKED_TRANSITIONS

- `human_review_pending → approved_for_future_promote` (always blocked)
- `approved_for_future_promote → promote` (always blocked)
- Any automatic approval (always blocked)
- Any unattended transition (always blocked)

## 7. BLOCKED_ACTIONS

- `production_write`
- `telegram_send`
- `timer`
- `collect`
- `generate`
- `git`
- `unattended_promote`
- `model_call`
- `media_generation`
- `real_approval`
- `auto_approval`

## 8. API_ENDPOINTS

### `GET /api/daily-digest/human-approval-transition-dry-run`

Read-only. Returns the dry-run result JSON.
- Tested during smoke test: returned `phase=5C-2C-C5N2`, `mode=approval_transition_dry_run_only`, `would_transition=True`, `real_transition=False`, `production_write_allowed=False`, `current_state=approval_pack_ready`, `proposed_next_state=human_review_pending`, `transition={from: approval_pack_ready, to: human_review_pending}`, evidence met 6/7.

### `POST /api/daily-digest/human-approval/transition-dry-run`

Token-gated + confirm-phrase-gated. **Re-runs the dry-run planner only**. Never modifies approval state, never approves, never executes, never writes production, never sends Telegram.
- Test 1 (wrong phrase): returned `result=blocked`, `blocked_reason=confirm_phrase_mismatch`. ✅
- Test 2 (right phrase "DRY RUN DAILY HUMAN APPROVAL TRANSITION"): returned `result=success`, `real_execution=False`, `real_transition=False`, `production_write_allowed=False`, dry-run with `would_transition=true`. ✅
- Audit log: `action_id=daily_digest_human_approval_transition_dry_run`, `risk_level=low`, `real_execution=false`. No token recorded. ✅

## 9. VALIDATION_RESULTS (14/14 PASS)

| # | Suite | Status |
|---|---|---|
| 1 | `check:daily-digest-human-approval-transition-dry-run` | ✅ PASS (planner ran, dry-run result written) |
| 2 | `validate:daily-digest-human-approval-transition-dry-run` | ✅ PASS (28/28) |
| 3 | `validate:daily-digest-human-approval-scaffold` | ✅ PASS (26/26) |
| 4 | `validate:daily-digest-continuous-promote-workflow` | ✅ PASS (27/27) |
| 5 | `validate:dashboard-control-safety` | ✅ PASS (12/12) |
| 6 | `dashboard:control:validate` | ✅ PASS (16/16) |
| 7 | `validate:daily-digest-promote-approval-pack` | ✅ PASS |
| 8 | `validate:daily-digest-promote-executor-disabled` | ✅ PASS |
| 9 | `validate:daily-digest-promote-execution-review` | ✅ PASS |
| 10 | `validate:daily-digest-promote-gate` | ✅ PASS |
| 11 | `validate:sanitizer-secret-completeness` | ✅ PASS |
| 12 | `validate:sanitizer-false-positives` | ✅ PASS |
| 13 | `validate:telegram-sanitizer` | ✅ PASS |
| 14 | `validate:project-report-send` | ✅ PASS |

## 10. SMOKE_TEST_RESULT

- Started control server on `127.0.0.1:18793` (in background).
- `GET /health` → `200 OK`, mode `localhost-only-dry-run-safe-readonly-confirmed-low-risk-hardened`.
- `GET /api/daily-digest/human-approval-transition-dry-run` → `200 OK`, returned `phase=5C-2C-C5N2`, `mode=approval_transition_dry_run_only`, `would_transition=True`, `real_transition=False`, `production_write_allowed=False`, `current_state=approval_pack_ready`, `proposed_next_state=human_review_pending`, `transition={from: approval_pack_ready, to: human_review_pending}`, evidence met 6/7.
- `POST /api/daily-digest/human-approval/transition-dry-run` with `confirm_phrase="WRONG_PHRASE"` → `result=blocked`, `blocked_reason=confirm_phrase_mismatch`. Audit: `confirm_ok=false`, `result=blocked`. No token recorded.
- `POST /api/daily-digest/human-approval/transition-dry-run` with `confirm_phrase="DRY RUN DAILY HUMAN APPROVAL TRANSITION"` → `result=success`, `real_execution=False`, `real_transition=False`, `production_write_allowed=False`, dry-run with `would_transition=true`. Audit: `confirm_ok=true`, `result=success`. No token recorded.
- Shutdown server cleanly. Port 18793 free.
- **No production paths written** during smoke test.
- **No Telegram sent** during smoke test.

## 11. PROTECTED_PATH_CHECK (md5-verified unchanged)

| Path | Hash (C5M1A baseline) | Current | Status |
|---|---|---|---|
| `reports/daily-digest.md` | `735002a3969746aefabc57c75b5220e8` | `735002a3969746aefabc57c75b5220e8` | ✅ UNCHANGED |
| `reports/telegram-digest.txt` | `53c2a73d440eb32967d1a9185763a6b3` | `53c2a73d440eb32967d1a9185763a6b3` | ✅ UNCHANGED |
| `dashboard/status.json` | `d98c500cb52c78f57edd941cdedc7b49` | `d98c500cb52c78f57edd941cdedc7b49` | ✅ UNCHANGED |

No production data was modified. C5N1 approval state was NOT modified. No rollback triggered (none needed).

## 12. MODEL_CALL_STATUS

NONE — pure JSON + TS source edits. Planner uses `fs` + `path` only.

## 13. GENERATED_MEDIA_STATUS

NONE — no images / videos / music generated.

## 14. LIMITATIONS

1. The planner does **not** read `process.env`, so `env_gate_evaluated` is conservatively reported as `false` in the result JSON. A real orchestrator (future phase) would verify the env var externally.
2. The `confirm_phrase_matched` evidence is `false` in the default CLI call (no phrase provided). To exercise the dry-run with the right phrase, the POST endpoint must be used with the exact phrase "DRY RUN DAILY HUMAN APPROVAL TRANSITION".
3. Even at `approved_for_future_promote` in a future phase, the actual production write is blocked by `production_write_allowed=false` and would require the C5M1 controlled promote executor to be invoked separately.
4. The 28-check validator uses precise regex patterns to avoid false positives from property names like `no_child_process: true` (a property flag) vs actual `child_process` module usage.
5. The dry-run result file is separate from the C5N1 approval state files. The dry-run planner only writes to the dry-run result file; it NEVER writes to the C5N1 state file.
6. The smoke test ran on port 18793 (not the default 18791) to avoid conflicts. The server was cleanly shut down.

## 15. NEXT_PHASE_PROPOSAL

- **C5N-3 (proposed only, not implemented)**: Real human-approval transition. Would require:
  - A new CLI: `cqa-approve-transition --run-id=<run> --from=<state> --to=<state> --approver=<name>`
  - The CLI would write an audit log entry with `real_transition=true` and `real_execution=true` ONLY AFTER the human-confirmed the env gate + checked the approval pack + verified the from/to state pair
  - The CLI would flip the approval_state to the new state and write a new transition history record
  - A new endpoint: `POST /api/daily-digest/human-approval/transition` (gated by token + confirm phrase + env gate)
  - All transitions remain manual-initiation gated; no timer / cron / auto-trigger.

## 16. Files Added / Modified

**Added:**
- `dashboard/daily-digest-human-approval-transition-policy.json`
- `dashboard/daily-digest-human-approval-transition-dry-run.json`
- `scripts/daily-digest-human-approval-transition-dry-run.ts`
- `scripts/validate-daily-digest-human-approval-transition-dry-run.ts`
- `reports/human-approval-transition-dry-run.md`
- `reports/telegram-phase-5c2c-c5n2-human-approval-transition-dry-run.txt`

**Modified:**
- `scripts/control-server.ts` (+2 endpoints)
- `dashboard/control.html` (+1 panel + JS loader)
- `package.json` (+2 scripts)
- `docs/PRIVATE_CONTROL_SERVER_RUNBOOK.md` (+1 phase section)
- `README.md` (+1 row)
- `ROADMAP.md` (+1 row)

## 17. Commit

- Branch: `master`
- Base commit: `670e7f6` (C5N1)
- New commit (this phase): `git commit -m "Phase 5C-2C-C5N2: Add human approval transition dry-run"`
- Push: `origin/master`

---

*辛 🔮 — 实操优先，落地为王。Phase 5C-2C-C5N2 完成。*
